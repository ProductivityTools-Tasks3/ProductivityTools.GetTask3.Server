using System;
using System.Collections.Generic;
using System.Text;

namespace ProductivityTools.GetTask3.Domain
{
    public enum BagType
    {
        //list will contain tasks which are more complex object than list
        GTask,
        //list only displays list of items for example list of elements to take to trip
        GList
    }
}
