using System;

namespace ProductivityTools.GetTask3.CoreObjects
{
    public enum ElementType
    {
        NotDefined = 0,
        Task = 1,
        TaskBag = 2,
        //pw: change it to attribute
        //List of tasks without deadline (no red color)
        TaskList =3,

    }
}
